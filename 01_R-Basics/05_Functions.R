# R Basics: Functions

# Create a vector
scores <- c(70, 75, 80, 85, 90)

# Calculate the mean
mean(scores)

# Calculate the sum
sum(scores)

# Count the number of values
length(scores)

# Find the minimum value
min(scores)

# Find the maximum value
max(scores)
